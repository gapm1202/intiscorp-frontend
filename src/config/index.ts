// Configuración de aplicación (valores por defecto)
// Ruta relativa a la carpeta `public/` donde puedes poner una firma predeterminada.
export const signatureDefault = '/signatures/default-signature.png';

export default {
	signatureDefault,
};

